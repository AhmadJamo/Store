```cshtml
@using MiniStore.Domain.Enums
@model MiniStore.Application.DTOs.Sales.SaleDetailsDto

@{
    ViewData["Title"] = "Sale Details";
}

<div class="container mt-4">

    <div class="d-flex justify-content-between align-items-center mb-4">

        <h2>Sale Details</h2>

        <a asp-action="Index"
           class="btn btn-secondary">
            Back
        </a>

    </div>


    <!-- Sale Information -->

    <div class="card mb-4">

        <div class="card-body">

            <div class="row">

                <div class="col-md-4">

                    <strong>Invoice Number:</strong>

                    <div>
                        @Model.InvoiceNumber
                    </div>

                </div>


                <div class="col-md-4">

                    <strong>Date:</strong>

                    <div>
                        @Model.Date.ToString("yyyy-MM-dd")
                    </div>

                </div>


                <div class="col-md-4">

                    <strong>Warehouse:</strong>

                    <div>
                        @Model.WarehouseId
                    </div>

                </div>

            </div>


            @if (!string.IsNullOrWhiteSpace(Model.Notes))
            {
                <hr />

                <strong>Notes:</strong>

                <p>
                    @Model.Notes
                </p>
            }

        </div>

    </div>


    <!-- Items -->

    <h4>Items</h4>

    <div class="table-responsive">

        <table class="table table-bordered table-striped">

            <thead>

                <tr>

                    <th>Product</th>

                    <th>Quantity</th>

                    <th>Sale Price</th>

                    <th>Gross Total</th>

                    <th>Discount</th>

                    <th>Discount Amount</th>

                    <th>Total</th>

                </tr>

            </thead>


            <tbody>

                @foreach (var item in Model.Items)
                {
                    <tr>

                        <td>
                            @item.ProductId
                        </td>


                        <td>
                            @item.Quantity
                        </td>


                        <td>
                            @item.SalePrice.ToString("0.00")
                        </td>


                        <td>
                            @item.GrossTotal.ToString("0.00")
                        </td>


                        <td>

                            @if (item.DiscountValue > 0)
                            {
                                if (item.DiscountType == DiscountType.Percentage)
                                {
                                    @($"{item.DiscountValue:0.##}%")
                                }
                                else
                                {
                                    @item.DiscountValue.ToString("0.00")
                                }
                            }
                            else
                            {
                                @:-
                            }

                        </td>


                        <td>
                            @item.DiscountAmount.ToString("0.00")
                        </td>


                        <td>
                            @item.Total.ToString("0.00")
                        </td>

                    </tr>
                }

            </tbody>

        </table>

    </div>


    <!-- Invoice Totals -->

    <div class="row justify-content-end">

        <div class="col-md-5">

            <table class="table table-bordered">

                <tbody>

                    <tr>

                        <th>
                            Subtotal
                        </th>

                        <td class="text-end">

                            @Model.Subtotal.ToString("0.00")

                        </td>

                    </tr>


                    <tr>

                        <th>
                            Invoice Discount
                        </th>

                        <td class="text-end">

                            @if (Model.InvoiceDiscountValue > 0)
                            {
                                if (Model.InvoiceDiscountType == DiscountType.Percentage)
                                {
                                    <div>
                                        @($"{Model.InvoiceDiscountValue:0.##}%")
                                    </div>
                                }
                                else
                                {
                                    <div>
                                        @Model.InvoiceDiscountValue.ToString("0.00")
                                    </div>
                                }

                                <small class="text-muted">
                                    Discount Amount:
                                    @Model.InvoiceDiscountAmount.ToString("0.00")
                                </small>
                            }
                            else
                            {
                                @:-
                            }

                        </td>

                    </tr>


                    <tr>

                        <th>
                            <strong>Grand Total</strong>
                        </th>

                        <td class="text-end">

                            <strong>
                                @Model.TotalAmount.ToString("0.00")
                            </strong>

                        </td>

                    </tr>

                </tbody>

            </table>

        </div>

    </div>

</div>
```
