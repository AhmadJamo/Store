@using System.Globalization
@model MiniStore.Application.DTOs.Security.TenantRoleEditDto
@{ var ar = CultureInfo.CurrentUICulture.Name.StartsWith("ar"); ViewData["Title"] = ar ? "إنشاء دور" : "Create role"; }
<div class="container-fluid py-3"><div class="mb-4"><h2>@ViewData["Title"]</h2><p class="text-muted">@(ar ? "أنشئ دوراً خاصاً بالشركة الحالية وحدد صلاحياته." : "Create a role for the active company and choose its permissions.")</p></div><partial name="_Form" model="Model" /></div>
